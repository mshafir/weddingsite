<svg width={{multiply paths.length letterSpacing}} height="{{options.fontSize}}">
  {{#each paths as |path i|}}
    <path stroke="black" stroke-width="2" fill="none" d="{{{path}}}" transform="translate({{multiply i letterSpacing}},0)" visibility='hidden' ></path>
  {{/each}}
</svg>
